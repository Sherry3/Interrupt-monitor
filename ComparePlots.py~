import os

exp = input("Enter input experiments : ").split()
plot_no = input("Enter plots : ").split()
scope = input("Enter readme_plot scope(local/global) : ")

for i in exp:
	os.system('python3 Plot.py ' + i + " " + plot_no + " " + scope + " &")


